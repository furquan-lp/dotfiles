# Dark (Kanagawa terminal) status colors. Sourced on top of tmux.conf —
# by tmux.conf itself at startup (by hour) and by uxterm-setcolor on
# live dark/light switches.
set-window-option -g window-status-current-style bg=color8,fg=color0
set -g window-status-style bg=default
